# ydah notes

`/notes/` は Andy Matuschak の「エバーグリーンノート」の考え方を実践する場。ちょっと調べたことやAIの調査結果を書き残す場所で、Obsidian Publish風のデザインになっている。ちゃんとした記事を書く場所ではないので、ここはラフなメモでよい。使い捨てのメモではなく、時間をかけて育ち、他のノートと繋がっていく知識のネットワークを目指す。

## Notesの設計原則

ノートを書く・編集する際は以下の原則を意識すること。

1. **原子性** — 1ノート1概念。複数の概念を1ノートに詰め込まず、別ノートに切り出して`[[...]]`で繋ぐ。
2. **概念指向** — 出来事の記録や単なる要約ではなく、概念・トピックそのものを主語にして書く。
3. **密度の高いリンク** — 新しいノートを作成する際は、`src/content/notes/*.md` を検索するなどして既存ノートの中に今回のトピックへの言及がないか確認すること。プレーンテキストで言及しているノートが見つかったら、そちらの記述も `[[新ノートのファイル名|表示テキスト]]` 形式でリンク化する。
4. **連想的オントロジー優先** — フォルダによる階層分類ではなく、`[[...]]`リンクと`#tag`による関連付けを使う。
5. **自分のために書く** — 前置きや読者への配慮は不要。ラフなメモでよい。
6. **追記後は章構成を見直す** — 既存ノートに見出し（`##`）を追記した場合、追記した節だけでなく、ノート全体の章構成が読みやすい流れになっているか（見出しの重複・順序の乱れ・冗長な繰り返しがないか）を確認する。必要なら既存の見出しの位置や粒度も調整する。

ノート本文を、一般読者向けの完成された技術記事やチュートリアルに整えすぎないこと。調査の途中で得た具体的な事実や、自分が気になった点を残す場所なので、短い段落や断片的なメモでもよい。

## 新しいノートを追加する手順

1. `src/content/notes/` 配下に新しい Markdown ファイルを作る。ファイル名は英数字とハイフンの kebab-case（例: `perl-signal-handling.md`）。大文字、アンダースコア、スペースは使わない。
2. 1行目に `# タイトル` の形でタイトルを書く。タイトルは日本語でよい（むしろ日本語で書く）。
3. 本文はMarkdown。コードスニペットを載せる場合はフェンス付きコードブロックで言語を指定する（`perl` や `sh` のように）。シンタックスハイライトが自動で効く。
4. 書き終えたら以下を実行してビルドし、手元で見た目を確認する。

   ```sh
   npm run dev
   npm run check
   npm run build
   ```

   開発サーバーは `http://localhost:4321/notes/` で確認する。ビルドでは `dist/` と Pagefind の検索インデックスが生成される。これらは手元のプレビュー用で、Gitにはコミットしない。
5. `src/content/notes/*.md`（ソースのみ）をコミットする。lefthookのpre-commitフック（`scripts/update-note-dates.mjs`）が、ステージされたノートに作成日・更新日を付与する。
6. mdのみの変更なら、ブランチを切ってPRを作らずに直接`main`へpushしてよい。テンプレート、Astroのページ、ビルドロジック、ワークフローなどコードに手を入れた場合は従来通りPRを作る。

## Zettelkasten的な相互リンク

1トピック1ファイルを基本に、ノート同士を`[[ファイル名(拡張子なし)]]`で相互リンクできる。

- `[[perl-signal-handling]]` — リンク先ノートのタイトルでリンクが張られる。
- `[[perl-signal-handling|シグナル処理の話]]` — `|`区切りで表示テキストを変えられる。
- ノートのタイトルに別名を付けたい場合はfrontmatterの`aliases`を使える。
- 存在しないファイル名を指定するとビルド時（`npm run build`）に警告が出る。リンクは壊れたwikilinkとして表示されるので気づける。
- リンクされた側のノートには「🔗 リンクされているノート」として自動でバックリンクが表示される。手動でメンテする必要はない。
- コードブロック・インラインコード・数式内の`[[...]]`はリンクとして解釈されない（コード例として安全に書ける）。

新しいノートを書くときは、関連しそうな既存ノートがあれば積極的に`[[...]]`でリンクしておく。過去に書いたノートの中に関連しそうなものがあれば、そちらにも`[[...]]`を足して繋げておくとよい（原則3「密度の高いリンク」）。

## ハブノート（MOC）

原子ノートとは別に、同じ話題領域に属する複数の原子ノートを見取り図的に束ねる「ハブノート」（Zettelkasten/LYT文脈での MOC: Map of Content）を作ってよい。ハブノート自身は深掘りした内容を書かず、配下ノートを`[[...]]`でリンクし、それぞれの位置づけ・違いを一言でまとめる役割に徹する。

- 同じ分野の原子ノートが3つ前後たまってきたら、ハブノート作成を検討する。
- 配下ノート側にも「## [[ハブノート名]]の中での位置づけ」のような見出しでハブノートへの参照を書き、双方向にリンクする。
- ハブノート本文には他のタグに加えて`#moc`タグを付ける。`/notes/tags/moc/`でハブノート一覧を一望できるようにするため。

## `#tag`の仕様

本文中の`#tag`はビルド時にタグページへのリンクへ変換され、同じタグを持つノート同士をタグ一覧経由で束ねる（`[[...]]`ウィキリンクとは別の、フラットな関連付け手段）。タグはfrontmatterではなく、本文に書いたものを正とする。

認識されるタグの実際の仕様は以下の通り。

- **使える文字**: `#`の直後は英字(a-z, 大文字小文字問わず)または日本語（漢字・ひらがな・カタカナなど、Unicodeの「文字」全般）で始める。2文字目以降は英数字・アンダースコア・ハイフンに加えて同じくUnicodeの文字が続けられる（例: `#戦国時代`、`#ai-agent`、`#claude_code`）。
- **数字・アンダースコアでは始められない**: `#1234`のようなIssue/PR番号や`#40262`のような参照番号をタグと誤認しないための制約。日付や連番を`#tag`として使わないこと。
- **英字タグは小文字に正規化される**: `#AI`と書いても`#ai`と同じタグ扱いになる。日本語タグは正規化されないので表記ゆれに注意する。
- **直前が「英数字」だとタグとして認識されない**: `foo#bar`のように単語に`#`が直接くっついている場合はURLフラグメントやC#のような固有名詞と区別できないため無視される。一方、句読点や括弧の直後（例: `文末。#tag`、`見出し）#tag`）はタグとして認識される。日本語の文はスペースなしで`#tag`を続けても構わない。見出し行（`# タイトル`）はタグにならない。
- **タグはコードブロック・数式ブロック内では展開されない**。コード例中の`#include`や`#!/usr/bin/env`などが誤爆することはない。
- 複数タグを付ける場合は`#tag1 #tag2`のように半角スペース区切りで並べる（本文末尾にまとめて置くのが通例）。
- `/notes/tags/`（サイドバーの「🏷️ タグ一覧」からアクセス可）で全タグをタグクラウド形式で一覧できる。タグページ・タグ一覧ページもビルド時に自動生成される。手動でのメンテは不要。

## ノート内のmermaid図

ノートではmermaid言語を指定したフェンス付きコードブロックを書くと、閲覧時にmermaid.js（jsDelivr CDNから必要なページだけ遅延読み込み）によって図としてレンダリングされる。ネットワーク構成図・シーケンス図・状態遷移など、文章やASCIIアートより図の方が伝わる内容には積極的に使うこと。

- mermaidブロックを含むノートのページだけがmermaid.jsを読み込む。
- ダーク/ライトテーマはページの初期テーマに合わせて描画される。
- 通常のコードブロックと同様、ブロック内の`[[...]]`や`#tag`はリンク・タグとして解釈されない。

## ノート内の数式（KaTeX）

ノートでは`$$...$$`（ディスプレイ数式）と`\\(...\\)`（インライン数式）のTeX記法を書くと、ビルド時にKaTeXでHTMLへ変換される。数学・信号処理・物理などのトピックでは、数式をコードブロックやユニコード文字で代用せず、この記法で書くこと。

- `$$...$$`は行単位のブロックとして独立した行に書く（複数行可）。`\\(...\\)`は文中に埋め込める。
- 数式を含むノートだけがKaTeXのスタイルを必要とする。
- コードブロック・インラインコード内の`$$`や`\\(...\\)`は数式として解釈されない（シェルの`$$`やコード例を安全に書ける）。
- 数式内の`[[...]]`や`#tag`もリンク・タグとして解釈されない。

## X（Twitter）投稿を発端にしたノート

X上の発言をきっかけにノートを書き起こす場合は、その投稿を主とした文章構造にすること。「Xでこう言っていた → それを踏まえてこう考えた」の順で書き、地の文で発言内容を要約・言い換えて済ませない。

- 投稿URLは出典として単独の行に貼る。
- このサイトでは現在、X公式ウィジェットへの自動埋め込みは行っていない。URLを貼っただけで本文の代わりになるとは考えず、必要な事実と自分の考察をノートに残す。
- 投稿の直後（または前）に、自分の考察を独立したセクション（例: `## 考えたこと`）として続ける。

## URLを渡されたときのノート化方針

技術記事のURLを渡されて「まとめて」「ノートにして」と言われた場合、その記事の性質によってノートの主語を使い分けること。

- **URL自体（その記事・発表・実装）が有益な情報の場合** — 記事のトピックそのものをノートの主語にする。具体的な手順・発表内容が主眼の記事はこの扱いになる。
- **記事が要素技術の紹介にとどまる場合** — 記事ではなく、紹介されている要素技術そのものをノートの主語にする。記事はあくまで概念の入り口として使う。
- 海外ブログの紹介である場合は、地の文で要約・言い換えて済ませず、原文の内容・構成を尊重して書く。自分の考察を足す場合は、原文の要約とは別セクションに分けて書く。

## 作成日・更新日

各ノートのMarkdown先頭には、次のようなYAML frontmatterが付く。

```yaml
---
created: 2026-08-17
updated: 2026-08-17
---
```

- `git commit`時にlefthookのpre-commitフック（`scripts/update-note-dates.mjs`）が、ステージされた`src/content/notes/*.md`について、新規ファイルなら`created`/`updated`を追加し、既存ファイルなら`updated`だけ当日の日付に更新する。
- 日付はデフォルトで`Asia/Tokyo`基準。必要なら`NOTE_DATE_TIMEZONE`で変更できる。
- ファイルの`stat()`のmtimeには依存しない。frontmatterが無い場合でも、フックが日付を補ってからコミットする。
- ノート本文やタイトルと同様、`created`/`updated`を手で書き換える必要は基本的にない。
- frontmatterの`title`は任意。省略した場合は先頭のH1をタイトルとして扱うので、先頭にタイトルを書く。

## 書き方のトーン

- ラフなメモでOK。前置きや読者への配慮は不要。
- 日本語で書く。
- 検証結果やコマンド例など、具体的な内容を優先する。
- 特定のプロダクト・技術・サービスを批判したり、けなしたりするようなニュアンスは入れない。合わなかった点を書く場合も、事実ベースで淡々と記述する。
- 根拠のある出典がない情報は載せない。ウェブ調査した内容を書く場合は、末尾に「出典」セクションを設けて参照元をリンクする。裏取りできなかった情報や記憶・推測だけの内容は書かないか、推測であると明記する。
- 調査結果をノートにするときも、読者向けの長い入門記事に膨らませない。必要な背景だけを書き、気になった概念は別ノートへ切り出す。

## Notesへの追記提案

「〇〇ってどういう用途なの？」のようなオープンクエスチョン（技術・プロダクト・地名などについての素朴な調査依頼）をされたら、調べて回答した後に、その内容を`/notes/`コーナーに書き残すかどうかを聞くこと。

## ノート切り出し後の深掘り提案

ノートを新規作成した際、その本文中に「まだ独立ノートになっていないが、深掘りする余地がありそうな概念」（説明の中で触れているだけの関連用語・隣接する概念）が含まれていないか確認すること。見つかったら、書き終えたタイミングで「〇〇についても別ノートとして切り出して調べられそうですが、書きますか？」のように提案する。ノートを分割・切り出した直後も同様に、そこからさらに切り出せそうな概念がないか確認する。

## 手を動かす実験

説明を読むだけでなく、実際にこのマシン上でコードを書いて動かし、体感してから書くことを積極的に行う。特にネットワーク・カーネル・コンパイラ周りのトピックは、動かしてみないと理解が浅くなりがちなので、可能な場合は最小限の実験コードを書いて動かすことを優先的に検討する。

- 実験の記録は、対象となる概念のノートに混ぜ込まず、`<トピック>-experiment.md`のような独立したノートに切り出し、概念ノート側からリンクする。原子性の原則（1ノート1概念）に従う。
- 実験ノートには、目的・使った素材（参照したリポジトリ等）・実際に躓いた点とその対処・実行結果・コードから読み取れることを書く。単なる感想ではなく再現可能な記録にする。
- 実験コードを実行する前に、何を試すか（実行するコマンド・触るデバイスやリソース等）を説明し、必ずユーザーの許可を取ってから実行すること。

## 知識を問う質問には検索してから答える

事実・知識を問うどんな質問（聞き覚えのない単語・固有名詞はもちろん、自分の知識だけで答えられそうに見える一般的な質問も含む）に対しても、憶測で即答せず、まず最低1回はWeb検索で裏取りしてから回答すること。学習データのカットオフ以降の新情報や、記憶違い・古い情報のリスクを避けるため。

検索しても自信の持てる結果に辿り着けなかった場合、「これはおそらく〇〇というジャンルの話だろう」という思い込みで検索語を狭めたまま何度も再検索しないこと。まずジャンルの決め打ち自体を疑い、固有名詞そのものだけで検索する・複数ジャンルにまたがる中立的な語を使うなど、検索語を広げて調べ直すこと。

技術的な質問では、可能な限り公式ドキュメント・仕様・論文・実装などの一次情報を優先する。ウェブ調査をノートに反映するときは、参照したURLを出典として残す。

## 病気・医療トピックは公的情報を重視する

病気・症状・治療法など医療関連のトピックを調べる際は、厚生労働省（および関連する国内の公的機関、e-ヘルスネット等）の情報を優先的な情報源として重視すること。海外の医学文献（NIH/PMCなど）は病態生理など国際的に共通する内容の裏取りに使ってよいが、診断基準・治療方針・薬剤の扱いなど国内の運用に関わる部分は国内の公的情報と突き合わせること。

## 初回セットアップ

Node.jsは`22.12.0`以上を使う。

```sh
npm install
```

開発サーバーを起動する。

```sh
npm run dev
```

変更を確認する場合は以下を使う。

```sh
npm run check
npm run build
```

ビルド後の出力は`dist/`に生成される。検索インデックスはPagefindが生成する。必要なら`npm run preview`でビルド済みサイトを確認する。`npm run search:dev`は検索インデックスを`public/pagefind/`へコピーして開発サーバーから検索を確認するためのコマンド。

## GitHub Pages

公開先は`https://ydah.github.io/notes/`。このリポジトリはGitHub Pagesのプロジェクトサイトとして動かすため、通常のbase pathは`/notes`。

- `astro.config.mjs`の`PUBLIC_BASE`がbase pathを決める。指定しない場合のデフォルトも`/notes`。
- `.github/workflows/deploy.yml`は`main`へのpushでAstroをビルドし、GitHub Pagesへデプロイする。
- GitHub Actionsでは`PUBLIC_BASE=/notes`、`SITE_URL=https://ydah.github.io`を指定している。
- トップページは`/notes/`、ノート一覧は`/notes/notes/`、個別ノートは`/notes/notes/<slug>/`。
- `dist/`、`.astro/`、`public/pagefind/`などの生成物はコミットしない。

## 関連ファイル

- `src/content/notes/*.md` — ノートのMarkdownソース
- `src/content.config.ts` — Content Collectionsのfrontmatterスキーマ
- `src/lib/notes.ts` — ノート一覧、wikilink、タグ、バックリンクの処理
- `src/lib/note-index.mjs` — ノートのslugとタイトルを扱うインデックス
- `src/lib/remark-wikilinks.mjs` — `[[...]]`ウィキリンクの変換
- `src/lib/remark-hashtag.mjs` — `#tag`の変換
- `src/pages/notes/[slug].astro` — 個別ノートのページ
- `src/pages/notes/index.astro` — ノート一覧
- `src/pages/tags/` — タグページとタグ一覧
- `src/layouts/BaseLayout.astro` / `src/components/` — 共通レイアウト、サイドバー、テーマ切替、検索
- `astro.config.mjs` — Astro、base path、sitemapの設定
- `.github/workflows/deploy.yml` — GitHub Pagesへのデプロイ
- `lefthook.yml` / `scripts/update-note-dates.mjs` — pre-commitフックによる作成日・更新日の自動付与
- `README.md` — セットアップと公開先の概要
